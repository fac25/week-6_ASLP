import prisma from '../../../lib/prisma'

// PUT /api/products/:name
export default async function handle(req, res) {
    const postName = req.query.name
    const post = await prisma.plant.update({
        where: { name: postName },
    })
    res.json(post)
}
